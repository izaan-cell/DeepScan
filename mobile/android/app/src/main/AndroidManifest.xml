<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Reaches the desktop engine on the local network only — DeepScan
         never calls out to the internet. See mobile/README.md. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="false"
        android:label="DeepScan"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.DeepScan"
        android:usesCleartextTraffic="true">
        <!-- cleartext allowed deliberately: talking to a plain-HTTP engine
             on the local LAN (e.g. http://192.168.1.42:51424), never the
             public internet. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DeepScan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
